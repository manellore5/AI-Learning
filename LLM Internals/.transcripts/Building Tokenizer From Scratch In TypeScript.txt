Source: https://www.youtube.com/watch?v=mRcf5qQSYws
Title: Building Tokenizer From Scratch In TypeScript
Channel: Mehul Mohan (codedamn) · Published 2026-01-07 · ~40 min
(Transcript from YouTube auto-captions, cleaned/deduped.)

These large language models have changed the world. We can agree on that. At least for the professional work for people who are online all the time. They are using chart GPT, Claude Code, Enthropic, OpenAI, all of these companies almost all the time. Now, as a builder, I was super curious on the underlying technology. How do they actually work? Like, you know, it seems like magic if you think about it. You're writing to a system somewhere on the other part of the planet and it's just making sense of what you are writing and it's giving you not only just English but it's actually giving you real code or Japanese or Hindi or any other language that you are prompting into it right let alone being images or you know voice or video also which is like another dimension of its own. So I got to work and I figured out like how can I learn about this on a technical level without actually making it super confusing for web developers or for people who are don't have really machine learning background. So this is going to be a minieries. I'll try to keep them these videos short and I'll try to keep them interesting and I'll try to not deviate away from too much from the mainline audience that I know like I have web developers and people who have majorly worked with front end backend fullstack database those sort of technologies. So we'll not go into a lot of mathematics. We'll not go into a lot of statistics and stuff. But we'll try to keep it simple and we'll try to explain you in this video how tokenizer work. So let's take a look at what exactly we are going to look in this video. Make sure you leave a like and subscribe to the channel and hype the video before you move forward if you want to see the next part soon. First of all, I'll just give you I'll show you how this works. Like if I send this hello, you would see that it responds me with hey, what's up? which is like I mean just take a minute to appreciate the magic that's going on behind something like this where you are able to talk to computers finally like you are able to talk to a normal person right obviously this is not exactly like how you would talk to a normal person but it's close enough for a lot of people so anyway coming back to the point here you have your hello as a text right which is going now if we talk about it what's going on over here is something like an API chat post request over here right and this goes to an open AI server Now what you have to understand at this specific point is that this OpenAI server consists of multiple parts. One of them is supposedly called as tokenizer. Right? So this tokenizer is something that takes your hello which is the string whatever you have passed and it converts it into a bunch of numbers. Right? which is basically what the actual LLM like the large language model LLM see. So your large language model actually never really sees this hello as a string. Whatever this is, we'll discuss this eventually at some point in future. But whatever this model is, whether that's GPT5, Claude, Sonnet, Opus, anything like any of the LLMs that you can pick off, they never really see strings. They see a bunch of numbers. And this tokenizer's job is to convert these the string into a bunch of numbers. Now you might be thinking that it doesn't sound like a very hard job to do. We'll get to that. We'll get to a few reasonings. But imagine that this tokenizer is actually a complex piece of technology which is separate from the LLM itself. So large language model when we talk about GBD5, we are usually talking about this in most conversations also includes tokenizer itself. But tokenizer is a different part. So you can technically use just a tokenizer which we'll be building in this video outside of a large language model itself. Right? So to summarize this part, your large language model actually never really sees the numbers uh the text. It only sees these numbers which will then eventually be converted into binary and you know matrix multiplication and all of that which will happen for the LLM to give you a proper response. But we'll get to that stuff later. So right now our focus on this what is this tokenizer layer and how is this working. All right. Right. So let's get to coding now. So I have a simple project with me over here. I've just created a tokenizer folder. It's a simple TypeScript project. So we'll build a very simple tokenizer in Typescript using some of the data which we'll train it on. And I'll just show you step by step what exactly we are doing. So first things first, let's get back to our string which was hello world. Let's say over here. If I talk about something like this as a string, you might be thinking Mayul that you're saying that the string has to go through a tokenizer first and then it'll go into a large language model, right? Why do we need even a tokenizer? Like we already computers already store the string as a number, right? So this h would be stored as a number then e then l o and then so on. Right? So this fundamentally is in a number already. We have solved this problem in past. Then why do we need to do this again? Now the thing is the way it works is that large language models have a finite vocabulary finite set of vocabulary which they use to train that is something that they train upon as well but they are also something that they respond in right so right now I mean you and I we are humans we can think of responding in individual characters also right if I ask you like you know spell out your name but in single letters so you'll be able to say m e h u l m o h a n with llms we'll talk about in depth why this is in later videos. But with LLMs, the thing is that right now if you don't do that, if you train them on individual tokens, which is like individual characters, it's going to be extremely inefficient for LLMs to absorb the whole attention. It it will be very inefficient for LLMs to absorb the whole context of the incoming message as well as when they respond. So it will not be very computationally feasible for LLMs to work on byte streams which is what you know these characters are like if we consider this as like the simple character set like UTF8 simple English characters. So this M is one bite, E is one bite, H is one bite. So instead of doing this what happens inside a tokenizer or why we need a tokenizer in the first place is that we group together a bunch of these you know characters or you know based on the training how it happens is that we will group them together. For example, me hu is one token. Then L is one token. I'm just giving you an example. L space is one token. Let's say then you have MO as one token and A N as one token. I'm just giving you an example. So when you have something like this, this is computationally better for large language models to work with. That is all you have to remember right now. As we move forward in this series, we'll get to actual reasoning like why really it doesn't make sense for LLMs of today to actually, you know, work on a single level character or a bite level character. But what you have to understand at least for now is that it's not computationally feasible for LM to work of the current mode to be on a bite level stream or a single character stream level. That's why they create these bunch of tokens. Right? Now again coming back to tokenizer because we have missed a bunch of things. What this does fundamentally let me show you first of all how a tokenizer works. So there is this interesting web URL that you can visit tick tokenizer.el.app which gives you the ability to select a tokenizer right. So let's say we go with GPT2 which was one of the earliest smart models that we have and let's say I had add a text called hello world right. So over here you see on the right side you see that hello is highlighted as a blue token and then space world is highlighted as another token as a yellow token and we have these numbers 31 373 and 995. So these numbers are basically the digits that I was talking about right? So if you send something like hello world to GPT2 as an large language model then GPT2's tokenizer which is trained specifically for GPT2 will go ahead and it will convert this hello world into these two numbers and this is what gets forwarded to the large language model inside right so this is what fundamentally GPT2 over here sees it just sees these bunch of two numbers and then how it works how that operation happens how do you get like a response out of that that's a different story but this is what the tokenizer sends the actual ual large language model the same thing if we convert to let's say the CL 100K base which is like a different tokenizer you will see that the token count is still same but the token numbers have changed right depending on how your tokenizer is trained on the data on the vocabulary size that you are choosing and so on these numbers will depend on that but the general idea is that the this token is same right let's say if I ask it to give me a simple palen drum checker in typescript so if I ask it to give like ask GB to give a simple palendrome checker code example in Typescript and I paste this over here. You would see that the token count in this is 87, right? Which is the CL00K base tokenizer. So the CL 100K base tokenizer is the one that GPT4 uses, right? Or used to use before GPD5 took over and this O200K base is something that GPD5 uses and so on. So these are different tokenizers. There are different ways of converting your whatever string that you are passing into these bunch of numbers. But what I want you to see is that if we shift this to GPT2 for example, you see that the token count is 138. Right? And if we shift this to GPT4, the token count drops to 87. Now again the thing is over here coming back to my same point like why don't we have like a character level tokenizer because if you see if we take a look at character level tokenization then you will see that this will be much much larger right because the there are a lot more characters compared to 87 tokens which is effectively what is happening right now. The thing with LLMs is that the more number of tokens there is, the less efficient your LLM would be able to perform attention-wise, right? So just a dumb down explanation of this. If you have seen these LLMs, you would see that a lot of these LLMs have context size, window size, right? Like you cannot have more than 100,000 tokens or 200,000 tokens. And that is what it actually means, right? That an LLM at any given time is able to see through or scan through a finite number of tokens. This is the issue that we have right now with single character tokens is that if you have a lot of single character tokens, it'll become very very inefficient for an LLM to produce the output by actually taking a look at what's going on. At the same time, if we go back to GPT2 for example over here, you would see that in terms of GPT2, the token count is larger because it's chunking the text more, right? and it's chunking the text more because this is not a good tokenizer because it's it's it's spanning like its attention towards a lot of tokens, a lot of blocks, right? So, if you look at GPD2 versus GPD4's tokenizer, this is much more efficient in terms of tokens. So, it's not using as many tokens. You can see like in GPD4, this whole space block is just a single token, right? But in case of GPT2, if we go back, you will see that GPT2 considers all these spaces as different spaces, right? which again like I mean it is a waste of you know tokens if you're considering all of them as different spaces because in typescript at least it doesn't really matter right it's not like a space sensitive thing so this false might as well could be you know just here and it'll still be the same code but you are effectively like you know increasing tokens with every single space over here so anyway like those are like sort of the improvements that have happened but the general idea is that you want your tokenizer to create tokens with less count because if they are less in number it makes the performance of LLM just because it's chunking the code or whatever your snippet that you have written in such a way where it understands from its training data that I have seen this data in the past right so this input for example it's a single token because when this tokenizer was getting trained the LLM had actually seen this right that's why it is like a separate token which we'll get to at some point like in this video itself all right so let's start with the implementation now and a few things you will also understand along the way as we are doing it so what I'm going to do is go back here and I'll ask it. Can you give me a small paragraph which has a lot a lot of English plus uni code things that I can use for a tokenizer example. Right? So I'll just ask RGBD to give me sort of like a string where there are a bunch of these things Hindi and it's giving me emojis and everything. So I'll just copy this and I'll go back to my TypeScript and I'll just start working on this. Right? So let's say we have a string over here which is something like this. Right? And maybe we can just get rid of the extra stuff that we have here. Right. And now what we want to do is we want to see first of all the bite level representation in uni code like what this is exactly and how this is represented. Right? Now with UTF8 character set right this UTF8 and uni-ode is basically this is a character set. This is an encoding of uniode which supports all of these popular characters like and different different languages. Right? So if we go ahead let's say and if I say buffer.front from string and I give it like a UTF8 because this is UTF8 encoding. And if I console log this buffer, you will see that if I run this, you will see we get these bunch of hex bytes over here, right? And if you just spread this across, you will see that you will start to get these integers. So these integers are basically nothing but the representation of these individual characters, right? So if I remove this for example and I remove these also and I run this again you will see that the first that we get is 73. Now 73 is basically an asci code for the capital I and you can verify this if you write something like string from character code and let's say we add 73 over here right and just to give it like an X and you will see that for this console log the second one we get I again so this effectively is turning it into like a bunch of numbers which is something that you might think we want but again just like I explained it would be possible if LLMs work directly on character level encoding but that is not what LLMs work on because of computation reasons. So we need to create a tokenizer. We need a system that takes the string and outputs something like this but not exactly this. So it needs to be a scheme that LLM also understands. And then the LLM also responds back in the same way. Right? So the LLM also does not speak English or Hindi or any of that immediately. It'll respond back in this exact same way and it'll you know the tokenizer is the one that will then decompress or you know just change the output from these numbers to actual response that we are able to read on the screen. All right. So here you see that over here once we have this buffer if I give it like length as as this you will see that right now we have 530 characters or let's say tokens if assuming like one character you are saying as as one token. Once we are done with the tokenization, let's say we are just training our tokenizer on this text itself and we are running it through again. Once we train the tokenizer and then we run on this, you will see that it will be much smaller than this because tokenization is nothing but compressing the text in a specific way, right? Because if you look at this, what you're effectively saying is that this text, this function, this keyword is replaced by a number called 1723. If you go ahead and do this manually with this buffer sort of thing, you will have like 1 2 3 4 5 6 7 eight numbers, right? Instead of those eight different numbers, you just have this one number, right? So, it's sort of like a compression which is done and then provided to LLM. LLM operates under the same tokenizer. It knows like what 1723 in its training. it would know like where does it map in the vector space and then it'll return you something which will which will again you know will be decompressed or you know would be reversed by the tokenizer itself to give you the real output. I'm repeating a lot of this stuff is because this is important stuff. So you should be able to understand why we are doing it the way we are doing it. All right. So the first step of the tokenizer itself on the data that we are training whatever that data is is to figure out which sequences can be replaced right and remember that let's say if I have something like you know or even let's just take this example only that we have. So you see over here let's say I say that I is one of the sequence right I highlighted this and it's available in these three different places right over here. If I say something like ID over here it's not available at all anywhere right? So it will not be worth it for us to tokenize this because even if I change this to something like X as a new token, you will see that we can't do anything else about this right now. But for IL which I initially selected, if I do this and if I change this to X, we have three places where the same token can be used. So the compression is better here. Right? If I do this as a fourstar. So the first step in order to understand how do you tokenize is to find pair of characters that you would be able to replace with a new token. And this algorithm or this approach is called bite pair encoding. And the bite pairing encoding article on Wikipedia also lists the same thing. So you have like a bunch of these sequences where you start to replace the things that happen most often because that is how you will get to you know better compression. So you replace in this example like I gave you earlier you have a a which happens one two times right? So you just replace it with zed right? So it's a new token. Then you keep on doing this right? So again the zed itself becomes a new token itself. So you take the string and you just keep on doing this again and again until you are not able to compress this further with byte pairing encoding right and in order to decompress the data simply perform the replacements in the reverse order. So this is one of the first steps that we need to do both in training terms as well as in the terms of just you know encoding and decoding it for the LLM also. So I'm going to go ahead and I'm going to start pasting some code that I have already written while doing this learning of my own and I'm going to start pasting it over here and I'm going to explain you what all of that means. Right? So this function that I've created get pair stats. It's basically something that compresses this information. It it gives you this information. It not compresses it delivers you this information where it tells you that which pair occurs most and the frequency of that. Right? So the sorting at the end is what we do. So before I explain you this code, let me show you what this code actually does. Let's say we have this get payer stats and we have this buffer over here, right? Let's say if I pass this buffer as a number, sorry array. And if I console log this, let's say if I run this over here, you will see that we have a bunch of these numbers over here now. And we have something like this. So we have 224 and 164 as a pair occurring 10 times in this string, right? Whatever string that we are passing, then we have 32 and 226 occurring nine times, right? So this 32 is like an askai value UTF value for space and 2 to6 is also some character. Similarly for them similarly for them. So you see that this is all sorted out. So we have like this as the maximum values then this then this and so on and we will keep on replacing them as individual or as new token values. Right? So let's get into this function first of all to understand what's going on. So we need stats as an object. Right? So this is something that we create initialize it first. this numbers, this data over here is basically the raw buffer that I already showed you, right? So if we just console log this buffer, you will see that this raw buffer is nothing but the UTF8 value the character codes of individual characters over here, right? And if we get to these Japanese or if we get to these emojis, then these could be like multiple bytes. So it does not have to be just a single bite, it could be multiple bytes. Uni-ode UTF8 standard supports multiple bytes in a character, right? That's how we are able to represent like Hindi, Udu, these emojis, Japanese, Chinese, other all these mathematical symbols all of these sort of languages. Now, one thing you might have noticed is that all of these numbers are actually limited, right? So, they are not exceeding like you don't see any number in,000 or 5,000 or any of that. All these numbers have an upper bound of 256, right? And the reason for that is because like I mentioned UTF8 can have like four bytes and each bite consists of eight bits, right? So this bit which is there, this would be able to have a maximum value of 256, right? So all of these numbers that you see here can potentially have a maximum value of 255, not even 256. But because UTF8 supports like, you know, emojis and all, you can effectively combine up to four bytes together and create a new value out of it. There are certain semantics and standards that you have to follow with UTF8 which I'll not get into this. But the core idea is that UTF8 can support up to 1 2 3 four individual bytes and you can sum them up and you can create like a new value for a uni-ode character. This is why I'm also able to interchangeably use that you know 73 is the ASA value of I because ASI is an older system but UTF8 is compatible with that right. So even if this is UTF8 we can at least for the English and the numbers part and small letters capital letters we can say that this is the asai or uni code or UTF8. All right coming back to the function again you will see that we iterate over this whole array that we have until the last element. So we go from zero to n minus one which is you know the last we just leave the last element and the reason for that is that we are scanning for pairs. We are looking for pairs right? So we start with 73 and 100. We pick them. Then we pick 110 and 32. Then we pick 32 and 50. 50 and 48. 48 and 50. And so on and so forth, right? So we keep on doing this as a sliding window. Right? We just keep a window and we just keep on sliding it on next next numbers and we create this mapping over here. What this mapping is that it consists of a key value. Key is a string which is effectively not a string. It's just a but it's just two numbers combined with a dash. And then the value itself is the occurrences, right? The number of time it's happening. So you see that once we are done with this basically in the stats part if we console log this over here what you will get is this object which will list it in a way that you know these numbers or this pair occurs this time this pair happens this time these many times in this and so on and so forth right so this whole thing happens and then rest of the stuff that we are doing is just we are converting this object into an array and we are sorting it so that we get a value where we can just pick the maximum occurrence first right from that from that string that we have passed. So you can just pick the maximum occurrence and we'll complete the tokenizer from this specific thing because remember as we talked about in order to complete this tokenizer compression we need to go for things which are most repeated right so that would be the most efficient way of creating the tokenizer. Now once we have this get pair stats we can start to work a little bit on the tokenizer. So now I'm going to paste a second function that I created which is merge tokens right or it it could be probably you know named as like perform token swapping something like this because what we are effectively doing is that we are taking the existing tokens whatever it is we are getting a new merge pair which is something that we want to combine and what do we want to combine it with is the token ID right so you remember that you know earlier when we ran this we have these bunch of pairs, right? So, we want to replace this pair with something because that is when you know this thing will happen. This zed is this needs to be replaced with something, right? So, this 224 and 164 in our example will become something. What will it become? That would be the new token ID, right? So, it'll be again a number. It will not be a character or string or any of that. It'll be we are now operating only in numbers from this point onwards. So, it'll become a new number over here. We will pass this whole buffer over here inside tokens and the merge pair would be the one which is here. Right? So effectively what we are saying is that hey take this 224164 scan this whole buffer and replace this 224164 with some number that we are specifying in token ID. Now what this number would be this number would be at least more than 255 right and the reason for this is because we anyway have character sets we anyway have these numbers till 255 we can't use numbers up to 255 because they are part of you know this array itself because UTF8 standard uses one bite and this one bite can have value up to 255 right 8 bits again coming back to that so this number that is there has to be more than 255 5. So the minimum value is 256 itself, right? So the new token ID whatever you are replacing the number would be 256. Then in the next instance it'll be 257, 258, 259 and so on and so forth. Then you can train it up as much as you want. This is why when you go back to the tick tokenizer, you look at this function keyword. This is 1723, right? And the reason for this is because you know obviously this is a lot later in the in the training when GPT was getting trained and this tokenizer was getting trained. you will find this function in the in the sequence when the tokenizer was getting trained very later. Similarly, we have this at 17,000 this two lowerase thing right as a token. So this token again was when it was getting trained and we were just keeping on performing these replacements and so on and so forth. I don't know if they were using like a sequential way of increasing the token ID but if they were then assuming that they were then this occurred at 17,353 minus 255 because again we are not counting the initial first 255 characters because that is something that is anyway reserved by UTF8 right because those values could be there all right so I hope I have not confused you so far let's take it a step forward and let's keep on working on what we are doing right now it's going to get interesting real soon again let's talk about this function it's not very you know fancy it'll be very simple to understand what we do basically like I mentioned we take this whole array right over here this whole array we iterate over this again till the last element because we get the first number and we get the second number so we do the same sort of scanning right which I mentioned we go over through 7300 then we go go over through 110 and 32 so on and so forth so we keep on doing that and we see that if number one is merge pair of zero and if number two is merge pair of one for example you know let's say if we are supposed to replace 50 and 54 with some token. We'll keep on doing that this this and at some point we will come at this where it indeed is true. So once that becomes true, what we are doing is that we found the pair. We replace 54 over here with the new token ID. Let's say it's 256. So this becomes 256 and the next digit becomes null. Right? Now the process again repeats. Well, I think I've spotted a an optimization here. I would not say it is a bug, but you can actually increment I over here. Right? Because once this becomes the new token ID which is 256 and this becomes null, you don't have to scan the null again because we know for a fact that this will not be the pair because our pair is supposed to be two numbers itself. So we lie to TypeScript here for for some time that it'll be null. It'll never be possible. We'll just remove it later. And we just we can increment I as well right even if you don't do that it'll not at least matter in this specific case because you'll just get number as null which will not pass the check anyway and it'll just keep on working. So once we are done with this activity, we finally just remove all the nulls because we don't want nulls over in this array and we just return the final tokens, right? So we get a clean array again but with replaced items. That is all this function is doing. Perform token swapping again very simple to use and understand and nothing no magic going on over here. You might ask like you know why are we not just deleting this here itself. You can technically do that but I chose to not do it so as to not mess up with the loop itself right because the length will change and then there might be some additional code that you might need to patch it across. You can do it. There are multiple ways to do about this. Uh this is just one of the ways that exists. All right. So what we have to do now is we have to write a function or write the actual tokenizer which can you know get trained upon the data that we have right. So let's say um let's say I write the function tokenize over here and let's say we extract out this data in a different file because we'll anyway need a different file later down the line right so we'll have data.txt txt over here and if we import fs and we do text [music] or string at least right so we have these buffer uh we have the string from this file now and we have these bytes which are available to us and now we want to train on these bytes right so first things first is that we have to figure out that how many times do we have to train one way is to you know just exhaustively train it which is what this is doing over here inside the Wikipedia example it says that this data cannot be compressed further by bite pairing and coding because there are no pair of bytes that can occur more than once. That is one way to go about. But if you look at you know LLMs like OpenAI training charge GPT and GPT 4 and 5, I think they possibly can't do that. They possibly can't exhaustively train. I might be wrong. I'm just taking this as a hunch from the learnings that I have got because there is so much data that they have to put a size to the vocabulary. They won't be able to just brute force and just keep on training it on the tokenizer part. I might be wrong on this but I do feel like they'll have like an upper limit on the size of the vocabulary that they choose to be. So in this case let's say we say that size of vocabulary is let's say 300 for now. So what that means is that we need iterations size of vocabulary minus 256. Now the reason we need these iterations is because remember that every time we call this perform token swapping two tokens get converted into one new token right. So every token swap results in a new token getting created. So every time I'm calling this loop whatever that will be one new token gets created. And if we need the size of total vocabulary to be 300 which is including the the original asai characters which is like the number 0 to 256 not asai not the just the you know one byte maximum value characters. So 0 to 255 is 256 characters. That means you can additionally use the rest of the you know vocabulary size over here. So I can say for let I zero I less than iterations required I ++ and here we have the loop with us right we'll also create a copy of this so that we can just keep on replacing these bytes later and we'll just get the sorted pair stats first of all in this which is get pair stats on tokens to operate on right so this will just give us a bunch of stats on which ones are the maximum use this is again like not implemented most efficiently over here I can just get rid of this But it'll at least give us the first pair which is the most common used, right? The most popular pair in that specific buffer that we have passed. The new token ID that we have to use would be I + 256 because again remember that we can't use anything up to 255. And then we say that the new buffer basically becomes saying that hey I want to merge I want to swap this tokens from the original tokens is the tokens to operate on. The merge pair is the first pair that we get. Right? So if you look at the sorted pair zero, you will see that it's an array of number and two numbers. Right? This one is the occurrence count and this one is the actual pair. So we need the first pair and the first element of that because this is what gives us the merge pair. Then we need a new token ID which is there. Right? So this new buffer is exactly like this tokens to operate on but this would have replacement of the most popular buffer over there. Right? So it's this new once we call this function and the new buffer appears the new buffer would be something like this the original string is like this right so effectively because we have already maintained a copy here we can just give this copy again to the same thing and we keep on doing this right and finally once this is over if we look at tokens to operate on and let's say if we look at the original byte size as well let's say original is bytes.length length and then final is tokens to operate on.length length right and let's say if I just run tokenize over here so if I run this you will see that we get a bunch of these logs which probably we can just comment out for now so you see that original length was 529 because we don't have a lot of text so we can add a bunch of text here but the final after 30 iterations that got reduced to 386 right so if we would have passed the LLM actual byte level information it would have taken 529 tokens technically but after whether the tokenizer has done its job or whatever like it has compressed it based on the algorithm that we have written it takes only 386 tokens. Now of course as you train it further with more and more data it will get much smaller in terms of tokens it can consume and depending on like what sort of data do you have right if you have a lot of English data for example which has a lot of this does as a keyword right this itself would become like a single token right because first of all this will become a token then this will become a token then this x5 will become a token so it will keep on happening right you will keep on compressing the data and depending on you know what languages or what emojis or what your data set is this would again affect the tokeniz er and the performance that is why you know people say in ML like data is important because see these sort of trainings happens on the data that you're providing it and the more data a specific thing as the more trained it is sort of for it right starting from the tokenizer example all right so this is fine but we still can't encode or decode new tokens or new data based on this right and the reason for that is because we are not storing the mapping because we also need a mapping to figure out that okay if we find this pair Here we need to encode it with this token and vice versa. So I'll create a new array which is merge dictionary ordered which is basically nothing but a representation of you know this pair which we are finding and the token ID right this new token ID which we have. So what I'm going to do is I'm going to say it over here that merge dictionary order.push sorted pair stats this gives us the you know most popular token again in that specific byte array that we have passed. This gives us the pair and this gives us the first element right and then it gives us the second element. You could have used you know something like 1 dot join with a dash but I didn't do that because you know in the types at least that we have given it as a number number right so we want to keep it like strict but anyway so this gives us a dictionary of sorts right and now if I go ahead and console log this dictionary also what you will see is that if I run this you will see that we have a bunch of these pairings over here so we have 224 164 which is the most popular pair and every time we found that we replaced it with 256 Right? Then 3226 was the next popular pair. We replaced it with 257 and so on and so forth. You see that at the end later down the line we are also seeing things like 265 and 216. So 265 was never really part of the actual bite array, right? The actual characters because we know for a fact that every value inside that is not more than 255. So how does this happen? Well, this happens because again like going back to the same Wikipedia thing, you can take existing tokens and then replace them with a new token again, right? So this 265 is effectively if you go back here, it's a it's this pair. So 217 31138 is what 265 is combined with 216, right? So as you keep on doing this, it'll keep on compressing more and more depending on the data that you have. Now once we have this merge dictionary available with us, we would be able to perform encoding and decoding of any arbitrary text. So let's first of all understand how the encoding would work right. So again I'll just paste a function over here that I have already written um which will encode any string that we have into this list of numbers that we are able to generate with our tokenizer right and if I pass in let's say encode and let's say encode hello world which is something you know which is not there in the original string that we had. If I run this, you will see that we get encoding something like this, right? And this has like one of our tokens, one of our, you know, sort of like a new token that we had. So let's take a look at how this encoding function actually works. So first things first, we convert this into bytes again from the string itself. So we convert it into raw bytes, UTF8 bytes. And then we go ahead and we iterate on the dictionary that we already had this one. Right? So we have this dictionary with us. Now remember that inside the dictionary inside the tokenizer because this is an order dictionary this contains the most popular tokens first. So we have to go in the order of the dictionary itself. Whenever we are able to find the most popular token what we will do is as we are scanning through the bytes themselves and as we are scanning through the dictionary itself for every token in the dictionary we'll go through the bytes first and we will see if we actually got that pair. If we did get that pair, we will just perform the replacement, right? But what I'm showing you right now is basically this function only if you just paid attention. Perform token swapping has basically the exact same signature as the one that we are doing in sort of encoding, right? Because we have to repeatedly keep on changing it based on the dictionary that we have. That's the only difference in this case, right? So we keep on replacing it. We just keep on scanning the bytes. We see that okay, we have 224 and 164 as the first thing that is there. And if our priority key which is this 224 and 164 matches the first bite and the second bite in the scanning that we are doing of the string we are good to replace. We replace it. We set the next bite is null because it will be removed later and then we skip the next bite because it's anyway going to be null. Right? So that is how we just encode it and then we finally just filter out the nulls and we just replace. So encoding [music] is done this way. Now let's come to decoding part. Right? So once we have let's say an encoding and once we decode this IDE should give us back the same string. So let's just say and paste the function again that I have for decoding. Let's just run this first then I'll explain you how this works. And you see the decoding of this encoded string gives us back hello world really right. So how does the decoding part work? Again with decoding we have tokens the numbers which are already encoding right encoded right? So we have to just do the reverse of it. So what we do is we create the tokens first then we create a reverse dictionary right because now we have the incoming token which is like 280 290 these are the things that we'll find in the dictionary in the in the byte stream right uh these tokens that are passed to us and those tokens have to decompose into two numbers because that is what the bite pair algorithm is. We have done it only for two numbers. So what we do is we construct this dictionary first. So we go through our merged dictionary and the order here is not really important, right? That is why it's just an object. So we go through our original dictionary. We take all these pairs that are there and what we do is we create a reverse dictionary. So we say that 289 corresponds to the first number as being 52 and the second number n2 being 53. Right? So we create this dictionary. Then we do the bite lookup. We loop over the whole thing again the tokens that are being passed to us which is basically this array. and we check for a lookup in the dictionary itself. Right? So once the dictionary is constructed, we will have all these numbers starting from 256 and then n1 into. So it's basically this thing only just the reverse lookup so that we don't spend a lot of time iterating on the array itself. Right? So we keep on doing that and if we find a lookup which is not null then we actually replace the first bite. So let's say we got to this 281, right? Which is not null. So we replace the first bite with the lookup n1 which is what 32. Then we push the next bite as well. So we push 119 just after this 281 as well. And then we decrement the i which is the loop variable because we want to process the same bite we have just replaced again. Now the reason for that is because these could be layered right like I mentioned 265 216 corresponds to 297 as a new token. Assume that 297 was there. This would decompose into 265 and 216. But we want to decompose 265 again because 265 itself corresponds to 217 and 138. Right? So that is why you will take the loop back a variable and it'll just keep on happening. And once this is done, the whole thing is done. You will just convert it to bytes directly to UTF8 because now we know for a fact that every number is under 255 if our dictionary and the setup is right. And you'll be able to just convert it to UTF8 properly, right? And then that's why you get like the right text hello world. So yeah, this is pretty much it for how the tokenizer works, how the encoding decoding works. This is a whole new thing for me as well. So I've also been doing a lot of learning myself. But I hope you liked this video because if you did, make sure you leave a like and comment below. Subscribe to the channel and as I'm learning about new large language model things, machine learning things, I hope to share this with you guys in the simplest way possible. I will see you in the next video very soon. [music]
